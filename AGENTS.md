# Agent Notes

## Git Workflow

- Before pushing changes from this repository, run `git fetch --prune` and inspect whether the remote branch has new commits.
- If the remote branch moved, integrate the remote changes before pushing; do not force-push unless the user explicitly asks.
- Keep skill changes focused and review `git status` and `git diff` before committing.
